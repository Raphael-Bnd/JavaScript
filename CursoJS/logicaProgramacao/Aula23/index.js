const num = 10;

if (num >= 0 && num <= 5) {
  console.log("Yeah, the number is equal or higher than zero and equal or below five.");
} else {
  console.log("Oh no, the number is below zero or higher than five.");
}
